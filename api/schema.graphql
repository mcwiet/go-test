schema {
  query: Query
  mutation: Mutation
}

type Person {
  id: ID!
  name: String!
  age: Int!
}

type Query {
  people: [Person!]!
  person(id: ID!): Person!
}

type Mutation {
  createPerson(name: String!, age: Int!): Person!
  deletePerson(id: ID!): Boolean
}
